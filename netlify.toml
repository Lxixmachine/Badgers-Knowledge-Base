[build]
  base = "Prototype development request"
  command = "npm run build"
  publish = "dist"
  # Netlify's default base-directory change detector incorrectly skips this site.
  ignore = "exit 1"

[build.environment]
  NODE_VERSION = "22"
